# pri nahrani backendu na VM
- v souboru `backend.py` nastav druhou main funkci, slouzici ta funguje pro zabezpeceny http
- v souborech `receive.js` a `script.js` nastav spravny websockety
- vsechno je oznaceny komentarema VM/local pro snadne vyhledavani (na VM pomocí vimu) 
- pote spustit `sudo python3 backend.py` a melo by se po prihlaseni do konzole vypsat: `WS client: Websocket opened.`